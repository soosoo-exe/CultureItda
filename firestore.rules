rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 모든 사용자가 데이터를 읽고 쓸 수 있도록 허용하는 규칙입니다.
      // 로그인 기능을 테스트하기 위한 목적으로 사용하세요.
      // 앱을 배포하기 전에 반드시 더 안전한 규칙으로 수정해야 합니다.
      allow read, write: if true;
    }
  }
}
